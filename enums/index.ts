export enum SocketEmits {
  Rooms = 'rooms',
  AddRoom = 'addRoom',
  EnterRoom = 'enterRoom',
  LeaveRoom = 'leaveRoom',
  Room = 'room',
}

export enum SocketNamespace {
  Rooms = 'rooms',
  Room = 'room-'
}